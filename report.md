# 基于 LangGraph 的分层异构长期记忆 Agent 项目报告

## 1 项目背景与解决的问题
大语言模型原生对话上下文窗口有限，普通对话 Agent 只依赖短期上下文，随着对话轮数增加，早期对话信息会被丢弃。现有记忆方案存在痛点：
- 只做简单向量检索，无法区分瞬时情绪和用户长期性格底色；
- 相同事件反复倾诉会过度放大情绪得分，没有事件相似度降权机制；
- 事实记忆、情绪画像、情景记忆混杂在一起，架构混乱，不利于扩展；
- Agent 无法感知用户情绪变化，不能区分 “临时心情不好” 和 “一贯情绪倾向”。

本项目实现分层异构长期记忆 Agent，分为三层记忆：结构化事实记忆、用户情绪画像记忆、向量情景记忆。基于 LangGraph 构建状态图工作流，能够记住用户基础事实、捕捉情绪变化、聚类相似事件，模拟现实人际交往：初期关心瞬时情绪，长期对话逐步沉淀用户性格画像。

### 1.1 开发环境
- 操作系统：Windows 11
- Python 版本：Python 3.11

**核心依赖库**
1. langgraph：构建 Agent 状态工作流
2. langchain‑openai：对接大模型 API
3. chromadb：向量数据库，存储情景记忆
4. sentence‑transformers：BGE‑small‑zh‑v1.5 中文向量化
5. sqlite3：本地结构化数据库，存储事实与情绪画像
6. python‑dotenv：读取环境变量
7. numpy：向量相似度计算

### 1.2 项目整体架构
三层异构记忆架构：
1. **结构化事实记忆层(SQLite)**：存储用户显式事实：姓名、爱好等硬事实，由 MemoryValidator 校验更新。
2. **情绪画像记忆层(SQLite + 向量相似度)**：存储 joy/sadness/anger/fear 四类情绪基线，实现时间衰减、事件相似度降权；区分瞬时情绪和长期情绪基线；相同事件多次倾诉做权重衰减，全新事件正常更新画像。
3. **情景向量记忆层(ChromaDB)**：保存原始对话片段，对话时做相似度检索，召回历史情景，给大模型作为参考上下文。

LangGraph 维护全局 State 状态，串联：事实提取节点 → 情绪更新节点 → 向量记忆写入 → 记忆检索节点 → LLM 生成回复节点。

## 2 核心模块设计
### 2.1 MemoryValidator 事实记忆模块
负责解析用户对话提取硬事实(姓名、爱好)，存入 SQLite，避免重复写入；过滤无效事实。

### 2.2 EmpathyFilter 情绪画像模块
本项目创新点：
1. **时间衰减机制**：`decay_factor =0.94`，每次更新，旧情绪数值自动衰减，久远情绪影响逐步降低。
2. **事件相似度降权**：新输入文本和历史事件向量比对，如果属于同一事件，更新权重降低，避免一件事反复说把性格画像带偏；全新事件使用完整权重更新基线。
3. **样本门槛**：设置最小基线样本阈值，对话轮数少时，不轻易生成稳定性格画像，模拟人与人刚认识阶段，只关注瞬时情绪。
4. 输出瞬时情绪、长期情绪基线、情绪偏移量，Agent 可以感知 “用户平时开心，此刻突然悲伤” 这种变化。

关键参数：
DECAY_FACTOR = 0.94
MIN_BASELINE_TOTAL = 4.0
SIM_THRESHOLD = 0.75

### 2.3 VectorMemory 情景向量记忆模块
基于 BGE‑small‑zh‑v1.5 中文 Embedding，ChromaDB 持久化存储用户每一轮对话片段；对话发生时，以当前用户 query 做向量检索，返回 top‑N 相关历史情景，送入 LLM 上下文。

### 2.4 LangGraph 状态图工作流
State 自定义字段包含：messages、raw_user_input、hard_fact、instant_emotion、retrieved_memory。
节点流转顺序：`START → 用户输入预处理 → 硬事实提取节点 → 情绪画像更新节点 → 向量记忆保存节点 → 向量记忆检索节点 → LLM 生成回复节点 → END`

## 3 关键创新点
1. **分层异构记忆架构**：结构化数据库处理事实与情绪画像，向量数据库处理无结构情景记忆，各司其职。
2. **事件相似度降权的情绪更新策略**：解决同一事件反复倾诉导致情绪画像失真问题。
3. **区分瞬时情绪与长期情绪基线**：Agent 可以捕捉情绪偏移变化，而不是只看单次对话情绪。
4. **样本门槛机制**：模拟人际交往，刚认识不武断判定性格，对话积累后才沉淀稳定画像。

## 4 系统测试
### 4.1 测试用例
| 测试编号 | 用户输入 | 说明 |
|---|---|---|
| T1 | 我最近和朋友吵架，心里特别难受 | 全新负面事件 |
| T2 | 还是因为和朋友吵架，我很难过 | 复述同一事件 |
| T3 | 今天出去玩，我很开心 | 全新事件，joy上升，观测情绪偏移 |

### 4.2 运行效果截图说明
第一轮用户初次陈述和朋友吵架内心难受，系统识别为全新事件，情绪权重取完整权重 1.0，sadness 情绪基线赋值为 1.00；
第二轮用户继续复述同一件吵架事件，系统通过 LLM 事件一致性判别识别为复述旧事件，触发降权逻辑，权重设置为 0.2。同时开启时间衰减机制，上一轮情绪数值先乘以衰减系数 0.94，再叠加本轮权重，最终计算得到 sadness=1.14。
向量检索模块正确读取上一轮历史对话，没有读取本轮尚未持久化的用户输入，验证了记忆写入后置的时序隔离方案有效性。

### 4.4 事件‑情绪联合更新模块实现
为了解决用户反复吐槽同一事件造成情绪画像虚高失真的问题，本系统采用双层事件识别策略：短期依靠大模型做事件语义判别，对连续复述同一事件做情绪权重降权；长期采用 HDBSCAN 向量聚类作为后备约束，识别间隔多轮会话之后旧事重提的场景，搭配事件冷却时间机制模拟人类心理变化。

为消除向量库读写时序带来的逻辑 bug，本工作对 Agent 图执行流程做时序隔离优化：向量记忆检索操作放置在本轮记忆持久化写入之前。LangGraph 工作流节点执行顺序调整为：事实瞬时情绪提取 → 向量历史检索 → 用户情绪画像更新 → 大模型回复生成，完整一轮对话全部执行完毕、输出 AI 回复之后，才将本轮用户文本写入 Chroma 向量库与 SQLite 备份表。该设计保证检索接口只会读取历史会话，不会读取当前轮还未完成的对话内容，避免检索到自身输入而造成事件误判。

情绪更新逻辑内置时间衰减因子 `decay=0.94`，每一次更新用户情绪画像时，原有四项情绪分数全部先乘以衰减系数，实现旧情绪随对话轮次逐步淡化。当识别到复述已有事件时，情绪叠加权重降低至 0.2；识别为全新事件则使用完整权重 1.0。同时设置基线样本门槛，对话样本不足时标记 “暂无足够历史”，不强行生成用户性格基线，避免小样本下画像失真。

HDBSCAN 聚类不会每一轮都执行，设置触发轮数参数`CLUSTER_TRIGGER_TURN=5`，每累计 5 轮用户对话才在后台执行一次聚类运算，降低推理开销。聚类输出事件簇编号`cluster_id`存入 SQLite 元数据表，下一轮对话的状态才会读取该簇编号；当命中已有事件簇，同样启用降权逻辑，并且记录簇最后访问时间，实现事件冷却机制。

### 4.5 记忆检索与回复生成模块
回复生成阶段严格开启防幻觉约束，大模型仅允许使用传入的硬事实、检索得到的情景记忆片段生成回答，禁止编造用户没有表述过的经历。用户情绪基线、瞬时情绪、情绪偏移描述仅用于调节回复共情语气，不允许直接输出性格标签，也不能基于情绪数值虚构用户故事。

当情绪偏移检测模块检测到用户瞬时情绪和长期性格基线出现明显反差，会将偏移描述送入 Prompt，引导模型感知用户当下的状态变化，模拟现实人际交往：不仅看到用户当下说了什么，还能感知 “平时状态稳定的人，此刻情绪发生明显变化”，以此提升陪伴对话的共情效果。

### 4.6 整体模块数据流总结
本系统构建三层异构记忆存储架构：
1. **SQLite 数据库**：存储用户稳定硬事实、事实变更历史、用户长期情绪画像、向量 embedding 备份、HDBSCAN 事件簇元数据；
2. **Chroma 向量数据库**：存储情景记忆文本与 BGE‑small‑zh‑v1.5 生成的向量，用于语义召回；
3. **LangGraph 状态内存**：保存单轮对话临时状态，包括提取出的事实 JSON、瞬时情绪、检索得到的记忆列表、情绪偏移描述，会话结束自动销毁，不做持久化。

完整数据流：用户输入 → LangGraph 状态接收 → LLM 提取硬事实与瞬时情绪 → 向量库检索历史记忆(本轮尚未写入) → 结合检索历史做事件判别、更新衰减情绪画像 → 把记忆、事实、情绪观测信息送入生成 Prompt → 流式输出 AI 回复 → 对话节点全部结束后，写入向量库、备份 embedding、条件触发 HDBSCAN 聚类、持久化硬事实更新。

## 5 系统测试与结果分析
### 5.1 功能测试用例设计
从三个维度设计测试集：全新事件输入、同一事件重复倾诉、多轮混合事件输入，分别验证时间衰减、事件降权、时序隔离、情绪偏移检测功能。

测试超参固定配置：
- 情绪衰减系数：0.94
- 全新事件权重：1.0
- 复述事件降权权重：0.2
- 情绪基线有效样本门槛：4.0
- 内存对话缓冲区最大长度：5

| 测试编号 | 测试场景 | 输入说明 |
|---|---|---|
| TC‑01 | 全新事件输入 | 用户输入一段从未出现过的事件表述，判定为全新事件，权重取1.0 |
| TC‑02 | 同一事件重复倾诉 | 针对已经出现过的事件，用户再次倾诉宣泄，识别复述事件启用0.2降权 |
| TC‑03 | 多件独立事件累积 | 依次输入多件互不相关的事件，情绪总分累积达到门槛开启偏移判断 |
| TC‑04 | 情绪偏移检测(情绪反差) | 先构建正向长期情绪底色，随后输入突发负面情绪，检测情绪反差 |
| TC‑05 | 情绪延续检测 | 长期处于某一类情绪倾向，继续输入同类情绪 |
| TC‑06 | 时间衰减验证 | 多轮交替对话，持续调用情绪更新接口验证衰减效果 |
| TC‑07 | 向量检索失效兜底 | 向量检索返回0结果，读取内存缓冲区完成事件判别 |

### 5.2 功能测试结果与分析
#### 5.2.1 TC‑01、TC‑02：同一事件降权测试
测试输入：
1. 我最近和朋友吵架，心里特别难受
2. 还是因为和朋友吵架，我很难过

测试结果：用例通过。第一次全新负面事件，sadness 直接增加 1.0；第二次复述相同事件，sadness 仅上涨 0.14。如果不开启事件降权，第二次同样采用 1.0 权重，sadness 会达到 1.94。由此可见，事件降权机制能够有效抑制单一事件反复倾诉对长期情绪画像的过度影响。

#### 5.2.2 TC‑03、TC‑05：多事件累积与情绪延续测试
依次输入朋友吵架、考试失利、社团压力等多件相互独立的负面事件，情绪基线持续累积。当情绪总得分超过 4.0 有效样本门槛后，继续输入同类负面表述，控制台输出：`[情绪偏移检测] 用户当前情绪延续已有倾向`。

测试结果：用例通过。系统识别用户长期已经形成悲伤倾向，当前瞬时情绪和长期性格底色保持一致，不触发情绪偏移告警，符合设计预期。

#### 5.2.3 TC‑04：情绪偏移检测测试
在对话中先通过正向语句构建用户乐观的长期情绪底色，再输入突发负面表述，模拟用户情绪反常变化。

测试输入序列：
1. 不过今天出去玩了一会，心情好多了
2. 但是晚上又突然很难过，心里堵得慌

测试结果：用例通过。当长期画像偏向积极，而瞬时出现较强悲伤情绪时，系统识别出二者存在明显反差，输出情绪偏移提示，实现感知用户情绪突变的效果，模拟现实人与人长期相处的感知逻辑。

#### 5.2.4 TC‑06：时间衰减机制测试
每次执行`update_emotion()`更新情绪画像，joy、sadness、anger、fear 四项历史情绪数值全部乘以衰减系数 0.94。较早对话产生的情绪不会永久固化，随着对话轮次增加逐步弱化。

测试结果：用例通过。该机制实现记忆随对话进程自然淡化，避免很久以前的情绪长期支配用户画像。

#### 5.2.5 TC‑07：向量检索失效兜底测试
部分对话轮次向量检索返回 0 条相关情景记忆，系统并未终止事件判别逻辑，读取内存缓冲区保存的最近 5 轮原始对话文本完成事件重复判断。

测试结果：用例通过。当 Embedding 召回效果不足时，依靠内存缓冲区兜底，提升整个模块的鲁棒性。

#### 5.2.6 时序隔离机制测试
向量记忆写入操作放在 Graph 对话流程全部执行完毕之后，调用`add_memory()`存入 ChromaDB。本轮用户输入内容，不会出现在本轮向量检索结果中。

测试结果：用例通过，解决本轮对话读取自身输入记忆的逻辑漏洞。

### 5.3 系统整体效果分析
本系统采用三层异构记忆架构，不同存储介质分工明确：
1. SQLite 数据库：存储姓名、爱好等结构化硬事实，以及 joy、sadness、anger、fear 组成的长期情绪画像；
2. ChromaDB 向量数据库：保存情景对话记忆，实现历史聊天内容的语义检索；
3. 内存缓冲区：维护最近 N 轮对话文本，作为向量检索失效时的事件判别兜底数据源。

基于 LangGraph 搭建 Agent 工作流，完整执行链路包含硬事实提取、事件语义判别、情绪画像更新、记忆检索、共情回复生成。测试结果表明，系统解决传统记忆 Agent 两个典型痛点：一是用户反复倾诉同一件烦心事，不会直接将用户判定为长期悲观；二是能够学习用户的性格底色，识别用户情绪的反常变化，让对话智能体具备长期陪伴式的感知能力。

### 5.4 系统存在不足
1. 衰减系数 0.94、样本门槛 4.0、复述降权 0.2 均为经验选取的超参，没有开展大规模对照实验完成参数寻优，超参取值会直接影响情绪画像输出结果。
2. 部分场景向量检索召回为 0，需要依赖内存缓冲区兜底；受 Embedding 模型性能限制，部分语义相近但字面差异较大的事件判别存在上限。
3. 情绪仅划分 joy、sadness、anger、fear 四类基础情绪，情绪粒度较粗；用户高频话题、表达习惯等隐式画像还未完整接入回复生成链路。

### 5.5 未来展望
1. 设计多组对照实验完成超参寻优，定量分析衰减系数、降权权重、样本门槛对画像输出的影响；更换效果更强的中文 Embedding 模型，优化向量召回效果。
2. 在现有情绪画像基础上增加用户话题、表达习惯隐式画像模块，让 Agent 输出语言风格更加贴合用户。
3. 扩充情绪类别，实现细粒度情绪识别；完善记忆遗忘策略，保障超长对话场景下记忆规模可控。

## 6 总结
本文针对对话 Agent 长期记忆模块现存问题，设计并实现一套三层异构记忆对话系统。针对单一事件扭曲用户画像、情绪永久固化、无法感知用户情绪反常变化等问题，提出事件语义判别加权降权、时间衰减、情绪偏移检测的解决方案。系统基于 LangGraph 搭建对话工作流，分别使用 SQLite、ChromaDB、内存缓冲区承载结构化数据、情景向量记忆、短期对话缓存。经过多组功能测试验证，系统可以区分复述事件与全新事件，抑制单一事件对画像的过度干扰；能够学习用户长期性格底色，识别情绪反常变化，具备类似真人长期相处的感知效果。系统仍存在经验超参、向量召回能力局限等不足，后续将通过参数对照实验、优化向量化模型进一步提升系统性能。

